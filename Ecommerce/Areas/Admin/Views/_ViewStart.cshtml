@{
    // Le decimos explícitamente a todas las vistas de esta
    // carpeta (y sus subcarpetas) que usen la plantilla
    // principal que está en la raíz.
    Layout = "/Views/Shared/_Layout.cshtml";
}